preprocess:
  test_size: 0.2
  random_state: 42

train:
  model: logistic_regression   # logistic_regression | naive_bayes | random_forest
  max_features: 20000
  ngram_max: 2
  imbalance_strategy: class_weight   # class_weight | smote | none
  random_state: 42

evaluate:
  cv_folds: 5
